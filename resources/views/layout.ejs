<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Real Time Pizza</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../scss/app.scss">
    <link rel="stylesheet" href="path/to/style.css"></link>
    <script src="path/to/index.var.js"></script>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poor+Story&display=swap');
        .logo{
            font-family: 'Poor Story', cursive;
        }
        .order{
            font-family: 'Poor Story', cursive;
        }
        
    </style>
     
</head>
<body class="">
   <nav class="container mx-auto flex items-center justify-between">
    <div class="ml-10 flex flex-row justify-between items-center">
        <a href="/"><img src="./img/logo.jpg" height="50" width="50" alt="Logo"></a>
        <h1 class="logo">Pizza</h1>
    </div>
    <div class="mr-2">
        <ul class="flex flex-row items-center  ">
            <li class="ml-6 hover:text-orange-600"><a href="#">Menu</a></li>
            <li class="ml-6 hover:text-orange-600"><a href="#">Offers</a></li>
            <% if(user) { %>
                <li class="ml-6 font-extrabold text-cyan-600 hover:bg-rose-500 rounded-lg py-1 px-1 hover:text-white"><%= user.name %></li>
                <li class="ml-6 hover:text-orange-600"><a href="/logout">Logout</a></li>
            <% } else {%>
                <li class="ml-6 hover:text-orange-600"><a href="/register">Register</a></li>
                <li class="ml-6 hover:text-orange-600"><a href="/login">Login</a></li>
            <% } %>
            
            
            <li class="ml-6 hover:text-orange-600 flex items-center">
                <span id="cartCounter" class="bg-orange-500 text-white font-bold px-2 py-1 rounded-full"><%= session.cart ? session.cart.totalQty :'1' %></span>
                <a href="/cart"><img src="./img/cart.jpg" class="" height="50" width="50"></a></li>
        </ul>
    </div>
   
   </nav>
   <%- body %>
</body>
<script src="../js/app.js"></script>
</html>